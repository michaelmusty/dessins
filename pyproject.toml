[project]
name = "dessins"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "michaelmusty", email = "michaelmusty@gmail.com"},
]
dependencies = ["lmfdb-lite[pgbinary] @ git+https://github.com/roed314/lmfdb-lite.git", "loguru>=0.7.3", "networkx>=3.4.2", "scipy>=1.15.3", "matplotlib>=3.10.3", "planarity>=0.6", "numpy>=1.24.0", "igraph>=0.10.0"]
requires-python = "==3.10.*"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = false
