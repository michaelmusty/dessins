[project]
name = "dessins"
version = "0.1.0"
description = "Default template for PDM package"
authors = [
    {name = "michaelmusty", email = "michaelmusty@gmail.com"},
]
dependencies = ["matplotlib>=3.10.3", "loguru>=0.7.0"]
requires-python = "==3.10.*"
readme = "README.md"
license = {text = "MIT"}


[tool.pdm]
distribution = false

[dependency-groups]
dev = [
    "ruff>=0.12.5",
]
